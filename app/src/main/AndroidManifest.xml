<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.henry.abaglobal">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        tools:replace="android:icon"
        android:allowBackup="true"
        android:icon="@drawable/abagloballogo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".FAQ">

        </activity>
        <activity android:name=".TermsAndConditions"/>
        <activity android:name=".ContactPage" >
        </activity>
        <activity android:name=".Abaglobal_help">
        </activity>
        <activity android:name=".UserPageActivity" />
        <activity android:name=".LoginActivity">

        </activity>
        <activity android:name=".RegisterationActivity"></activity>
        <activity android:name=".UserPageEditorActivity"></activity>
        <activity

            android:name=".Home"></activity>
            <!--android:label="@string/title_activity_home"-->
            <!--android:theme="@style/AppTheme.NoActionBar" />-->

           android:name=".Home"
            android:label="@string/title_activity_home"
            android:theme="@style/AppTheme.NoActionBar" />
        <!--<activity android:name=".Image1" />-->
    </application>

</manifest>